

#1. Lea sobre el resto de métodos de la clase File de Python [aquí](https://www.w3schools.com/python/#python_ref_file.asp) y cree una tabla donde explique qué hace cada uno. a 
    

| close() | cierra un archivo |
| --- | --- |
| detach() | retorno un flujo sin procesar desde el bufer |
| fileno() | retorna un numero que representa el flujo desde el sistema op |
| flush() | vaciar el bufer |
| isatty() | devuelve si el flujo es interactivo o no |
| read() |  retorna o lee el contenido del archivo |
| readable() | retorna si el flujo del archivo puede ser leido o no |
| readline() | retorno y lee linea por linea del archivo |
| seek() | cambia la posicion del archivo |
| seekable() | retorno si el archivo permite ser cambiado de posicion o lugar |
| tell() | nos dice la posicion o ubicacion del archivo |
| truncate() | cambia el tamano del archivo a tamano especifico |
| writable() | retorna si el archivo puede ser escrito o no |
| write() | escribe una linea de texto especifica en el archivo |
| writelines() | escribe una lista de lineas de texto en el archivo |